import 'package:flutter_zitherharp/flutter_zitherharp.dart';

const kSpacing = 8.0;

const kRunSpacing = 12.0;

const empty = '';

const combiner = '-';

const separator = '/';

/// Completely invisible color.
const invisibleColor = Color(0x44000000);
