import 'package:flutter_zitherharp/flutter_zitherharp.dart';

const edgeInsetsAll8 = EdgeInsets.all(8);

const edgeInsetsAll16 = EdgeInsets.all(16);

const edgeInsetsOnlyTop4 = EdgeInsets.only(top: 4);

const edgeInsetsOnlyBottom48 = EdgeInsets.only(bottom: 48);

const edgeInsetsSymmetricHorizontal8 = EdgeInsets.symmetric(horizontal: 8);

const edgeInsetsOnlyLeft16Right8 = EdgeInsets.only(left: 16, right: 8);
