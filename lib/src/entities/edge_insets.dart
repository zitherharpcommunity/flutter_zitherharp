import 'package:flutter_zitherharp/flutter_zitherharp.dart';

const edgeInsets0 = EdgeInsets.zero;

const edgeInsets8 = EdgeInsets.all(8);

const edgeInsets16 = EdgeInsets.all(16);

const edgeInsetsTop4 = EdgeInsets.only(top: 4);

const edgeInsetsBottom48 = EdgeInsets.only(bottom: 48);

const edgeInsetsVertical8 = EdgeInsets.symmetric(vertical: 8);

const edgeInsetsHorizontal8 = EdgeInsets.symmetric(horizontal: 8);

const edgeInsetsLeft16Right8 = EdgeInsets.only(left: 16, right: 8);
