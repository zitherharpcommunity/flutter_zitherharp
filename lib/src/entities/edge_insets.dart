import 'package:flutter_zitherharp/flutter_zitherharp.dart';

/// An [EdgeInsets] with zero offsets in each direction.
const EdgeInsets edgeInsets0 = EdgeInsets.zero;

/// An edge insets where all the offsets are `4`.
const EdgeInsets edgeInsets4 = EdgeInsets.all(4);

/// An edge insets where all the offsets are `8`.
const EdgeInsets edgeInsets8 = EdgeInsets.all(8);

/// An edge insets where all the offsets are `16`.
const EdgeInsets edgeInsets16 = EdgeInsets.all(16);

/// An edge insets with only top is `4`.
const EdgeInsets edgeInsetsTop4 = EdgeInsets.only(top: 4);

/// An edge insets with only bottom is `4`.
const EdgeInsets edgeInsetsBottom4 = EdgeInsets.only(bottom: 4);

/// An edge insets with only bottom is `48`.
const EdgeInsets edgeInsetsBottom48 = EdgeInsets.only(bottom: 48);

/// An edge insets with symmetrical vertical offset is `4`.
const EdgeInsets edgeInsetsVertical4 = EdgeInsets.symmetric(vertical: 4);

/// An edge insets with symmetrical horizontal offset is `4`.
const EdgeInsets edgeInsetsHorizontal4 = EdgeInsets.symmetric(horizontal: 4);

/// An edge insets with symmetrical vertical offset is `8`.
const EdgeInsets edgeInsetsVertical8 = EdgeInsets.symmetric(vertical: 8);

/// An edge insets with symmetrical horizontal offset is `8`.
const EdgeInsets edgeInsetsHorizontal8 = EdgeInsets.symmetric(horizontal: 8);

/// An edge insets with only left is `16` and right is `8`.
const EdgeInsets edgeInsetsLeft16Right8 = EdgeInsets.only(left: 16, right: 8);

/// An edge insets with vertical offsets is `4` and horizontal offsets is `8`.
const EdgeInsets edgeInsetsVertical4Horizontal8 =
    EdgeInsets.symmetric(vertical: 4, horizontal: 8);

/// An edge insets with vertical offsets is `8` and horizontal offsets is `16`.
const EdgeInsets edgeInsetsVertical8Horizontal16 =
    EdgeInsets.symmetric(vertical: 8, horizontal: 16);

/// An edge insets with vertical offsets is `12` and horizontal offsets is `16`.
const EdgeInsets edgeInsetsVertical12Horizontal16 =
    EdgeInsets.symmetric(vertical: 12, horizontal: 16);
